# Fix Windows path length issues with pnpm + React Native/Expo
# Hoisted layout mimics npm/yarn and reduces deep nesting
node-linker=hoisted
shamefully-hoist=true

# Ensure native modules are properly linked
public-hoist-pattern[]=*
